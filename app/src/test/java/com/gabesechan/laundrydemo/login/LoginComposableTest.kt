package com.gabesechan.laundrydemo.login

class LoginComposableTest {

    /*

    Not writing now due to issues with filenames and robolectric.  But we will want

Write LoadingButtonVerifier.  Verifies that enabled state matches, spinner state matches.

    * Test LoginInner, with enabled.  Should show the label text, and button disabled due to empty fields
    * Test typing just username.  Should not find the label, button still disabled
    * Test typing just password.  Should not find that label, button disabled
    * Test typing both.  Button should enable, no placeholders
    * Test typing both, click button.  Login func should be called.
    * Test enabled false, showSpinner true.  Verify button
    * Test non-zero error text.  Text should appear



     */
}